apiVersion: v1
kind: ServiceAccount
metadata:
  name: user-svc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-svc
      version: v1
  template:
    metadata:
      labels:
        app: user-svc
        version: v1
    spec:
      serviceAccountName: user-svc
      containers:
      - name: user-svc
        image: hashicorp/http-echo:latest
        args: ["-text=user-svc-v1", "-listen=:8080"]
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: user-svc
spec:
  selector:
    app: user-svc
  ports:
  - port: 8080
    targetPort: 8080
